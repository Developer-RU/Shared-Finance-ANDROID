<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SharedFinance" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar" />

    <style name="Theme.SharedFinance.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.SharedFinance.Splash.Post</item>
    </style>

    <style name="Theme.SharedFinance.Splash.Post" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">#000000</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
